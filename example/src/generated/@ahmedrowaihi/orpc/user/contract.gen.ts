// This file is auto-generated by @hey-api/openapi-ts

import { oc } from "@orpc/contract";
import * as z from "zod";

import {
  zCreateUserBody,
  zCreateUserResponse,
  zCreateUsersWithListInputBody,
  zCreateUsersWithListInputResponse,
  zDeleteUserPath,
  zGetUserByNamePath,
  zGetUserByNameResponse,
  zLoginUserQuery,
  zLoginUserResponse,
  zUpdateUserBody,
  zUpdateUserPath,
} from "../../../zod/user/schemas.gen";

/**
 * Create user.
 *
 * This can only be done by the logged in user.
 */
export const CreateUserContract = oc
  .route({
    method: "POST",
    path: "/user",
    operationId: "createUser",
    summary: "Create user.",
    description: "This can only be done by the logged in user.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "successful operation",
    inputStructure: "detailed",
  })
  .input(z.object({ body: zCreateUserBody.optional() }))
  .output(zCreateUserResponse);

/**
 * Creates list of users with given input array.
 *
 * Creates list of users with given input array.
 */
export const CreateUsersWithListInputContract = oc
  .route({
    method: "POST",
    path: "/user/createWithList",
    operationId: "createUsersWithListInput",
    summary: "Creates list of users with given input array.",
    description: "Creates list of users with given input array.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "Successful operation",
    inputStructure: "detailed",
  })
  .input(z.object({ body: zCreateUsersWithListInputBody.optional() }))
  .output(zCreateUsersWithListInputResponse);

/**
 * Logs user into the system.
 *
 * Log into the system.
 */
export const LoginUserContract = oc
  .route({
    method: "GET",
    path: "/user/login",
    operationId: "loginUser",
    summary: "Logs user into the system.",
    description: "Log into the system.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "successful operation",
    inputStructure: "detailed",
  })
  .input(z.object({ query: zLoginUserQuery.optional() }))
  .output(zLoginUserResponse);

/**
 * Logs out current logged in user session.
 *
 * Log user out of the system.
 */
export const LogoutUserContract = oc.route({
  method: "GET",
  path: "/user/logout",
  operationId: "logoutUser",
  summary: "Logs out current logged in user session.",
  description: "Log user out of the system.",
  tags: ["user"],
  successStatus: 200,
  successDescription: "successful operation",
});

/**
 * Delete user resource.
 *
 * This can only be done by the logged in user.
 */
export const DeleteUserContract = oc
  .route({
    method: "DELETE",
    path: "/user/{username}",
    operationId: "deleteUser",
    summary: "Delete user resource.",
    description: "This can only be done by the logged in user.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "User deleted",
    inputStructure: "detailed",
  })
  .input(z.object({ params: zDeleteUserPath }));

/**
 * Get user by user name.
 *
 * Get user detail based on username.
 */
export const GetUserByNameContract = oc
  .route({
    method: "GET",
    path: "/user/{username}",
    operationId: "getUserByName",
    summary: "Get user by user name.",
    description: "Get user detail based on username.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "successful operation",
    inputStructure: "detailed",
  })
  .input(z.object({ params: zGetUserByNamePath }))
  .output(zGetUserByNameResponse);

/**
 * Update user resource.
 *
 * This can only be done by the logged in user.
 */
export const UpdateUserContract = oc
  .route({
    method: "PUT",
    path: "/user/{username}",
    operationId: "updateUser",
    summary: "Update user resource.",
    description: "This can only be done by the logged in user.",
    tags: ["user"],
    successStatus: 200,
    successDescription: "successful operation",
    inputStructure: "detailed",
  })
  .input(
    z.object({ body: zUpdateUserBody.optional(), params: zUpdateUserPath }),
  );
