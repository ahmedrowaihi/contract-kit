---
"@ahmedrowaihi/aas-core": minor
---

New package: thin wrapper over `@asyncapi/parser` exposing `parseSpec` (file / URL / string) and `parseSpecOrThrow`. Mirror of `oas-core` for the AsyncAPI track.
