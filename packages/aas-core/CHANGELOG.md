# @ahmedrowaihi/aas-core

## 0.2.0

### Minor Changes

- 16676d9: New package: thin wrapper over `@asyncapi/parser` exposing `parseSpec` (file / URL / string) and `parseSpecOrThrow`. Mirror of `oas-core` for the AsyncAPI track.
